import {BrowserRouter, Route, Switch} from "react-dom";

const Router = () => {
    <BrowserRouter>
        <Switch>
            <Route exact path="/" component={TeamPicker}/>
        </Switch>
    </BrowserRouter>
}